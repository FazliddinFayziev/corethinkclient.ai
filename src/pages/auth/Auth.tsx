import React from 'react';
import { SignIn } from '../../components';

const Auth: React.FC = () => {
    return (
        <React.Fragment>
            <SignIn />
        </React.Fragment>
    )
};

export default Auth;